#
# OpenSIPS 住宅配置文件
#     由 OpenSIPS Solutions <team@opensips-solutions.com> 提供
#
# 这是一个基础的住宅（residential）配置文件。即可使用的 M4 模板位于
# examples/templates/ 目录下，可供快速部署参考。
#
# 有关可用语句、函数和参数的详细说明，请参阅 OpenSIPS 手册：
#      https://opensips.org/Documentation/Manuals
#


########### 全局参数设置 ##########

/* 取消下面的注释以启用调试模式（会输出大量日志）*/
#debug_mode=yes

# 日志级别：0-9，数字越大记录越详细
# 3 = LOG_INFO 级别，记录一般信息
log_level=3

# 模块级日志级别，与 log_level 类似，但用于 xlog 函数输出的日志
xlog_level=3

# 是否将错误日志输出到标准错误（stderr）
# no = 通过 syslog 输出，yes = 输出到控制台
stderror_enabled=yes

# 是否启用系统日志（syslog）记录
syslog_enabled=yes

# syslog 设施等级，用于日志分类
# LOG_LOCAL0 通常用于用户自定义应用日志
syslog_facility=LOG_LOCAL0

# UDP 工作进程数量，决定并发处理 SIP 消息的能力
# 根据服务器配置和预期负载调整
udp_workers=4

/* 取消下面注释以启用自动临时黑名单功能（默认禁用）
   当目标不可达时，自动将其加入黑名单，防止向已知不可用地址发送请求 */
#disable_dns_blacklist=no

/* 取消下面注释以启用 IPv6 DNS 查询失败后回退到 IPv4（默认禁用）
   适用于双栈网络环境 */
#dns_try_ipv6=yes


# SIP 监听套接字配置
# 格式：socket=协议:地址:端口
# 此处配置为在本机 UDP 端口 5060 监听
# 注意：生产环境通常需要修改为实际网卡 IP 地址
socket=udp:20.20.136.66:5060   # 请根据实际环境修改



########### 模块加载部分 ##########

# 设置模块搜索路径（module path）
# OpenSIPS 会从此路径加载 .so 模块文件
mpath="/usr/local/lib64/opensips/modules/"

#### 信令模块 (signaling)
# 功能：SIP 消息的信令平面管理
# 用途：提供信令传输的抽象，是大多数模块的依赖
loadmodule "signaling.so"

#### 无状态响应模块 (stateless)
# 功能：处理无状态的 SIP 响应（不创建事务）
# 用途：直接回复 SIP 响应，如 200 OK、404 等
loadmodule "sl.so"

#### 事务模块 (transaction)
# 功能：SIP 事务层管理（有状态处理）
# 用途：管理 INVITE 事务的创建、计时器、重试等
# 这是核心模块，支撑呼叫控制等高级功能
loadmodule "tm.so"

# tm 模块参数配置：
# fr_timeout: 非 INVITE 请求的最终响应超时时间（秒）
modparam("tm", "fr_timeout", 5)
# fr_inv_timeout: INVITE 请求的最终响应超时时间（秒）
modparam("tm", "fr_inv_timeout", 30)
# restart_fr_on_each_reply: 每次收到回复时是否重启计时器
modparam("tm", "restart_fr_on_each_reply", 0)
# onreply_avp_mode: 回复处理模式
modparam("tm", "onreply_avp_mode", 1)

#### 记录路由模块 (record route)
# 功能：在 SIP 消息中添加 Record-Route 头域
# 用途：确保后续请求（如 BYE、ACK）通过相同路径代理
loadmodule "rr.so"

# 不需要向 Record-Route 添加 From tag（此脚本不需要此功能）
modparam("rr", "append_fromtag", 0)

#### 最大转发次数模块 (max forward)
# 功能：处理 SIP 头中的 Max-Forwards 字段
# 用途：防止 SIP 消息在网络中无限循环
loadmodule "maxfwd.so"

#### SIP 消息操作模块
# 功能：提供对 SIP 消息头和正文的操作函数
# 用途：如添加/删除头部、修改 URI 等
loadmodule "sipmsgops.so"

#### FIFO 管理接口模块
# 功能：提供 FIFO 文件接口用于 OpenSIPS 管理
# 用途：允许外部程序通过文件系统与 OpenSIPS 通信
# 常用于命令行工具如 opensipsctl
#loadmodule "mi_fifo.so"

# FIFO 文件路径
#modparam("mi_fifo", "fifo_name", "/run/opensips/opensips_fifo")
# FIFO 文件权限（八进制）
#modparam("mi_fifo", "fifo_mode", 0666)

#### 用户位置模块 (user location)
# 功能：维护 SIP 用户的位置注册信息（URL 绑定）
# 用途：实现 SIP 呼叫的当前位置查询（类似 DNS）
# 数据存储在内存中，适合单实例部署
loadmodule "usrloc.so"

# NAT 标志名称，用于标识经过了 NAT 的用户
modparam("usrloc", "nat_bflag", "NAT")
# 工作模式预设：单实例无数据库（内存存储）
modparam("usrloc", "working_mode_preset", "single-instance-no-db")

#### 注册服务器模块 (registrar)
# 功能：处理 SIP REGISTER 请求，管理用户注册
# 用途：存储用户当前位置到 usrloc 模块
loadmodule "registrar.so"

# TCP 持久化标志，控制是否保持 TCP 连接
modparam("registrar", "tcp_persistent_flag", "TCP_PERSISTENT")
/* 取消下面注释以限制每个 AOR（地址-of-record）最多 10 个联系地址
   这是一种安全措施，防止注册洪水攻击 */
#modparam("registrar", "max_contacts", 10)

#### 计费/通话记录模块 (accounting)
# 功能：记录通话事件（启动、通话结束、丢弃等）
# 用途：生成 CDR（通话详单）用于计费或分析
loadmodule "acc.so"

# 是否记录早期媒体（early media）事件
modparam("acc", "early_media", 0)
# 是否报告被取消的呼叫
modparam("acc", "report_cancels", 0)
# 是否检测请求方向（用于正确处理计费）
# 启用此参数时，需要确保 rr 模块的 append_fromtag 也已启用
modparam("acc", "detect_direction", 0)

# 加载 UDP 协议处理模块
loadmodule "proto_udp.so"

#### 数据库文本驱动模块 (db_text)
# 功能：基于文本文件的数据库引擎，用文本文件模拟数据库
# 用途：用于不需要 MySQL 等数据库的场景，用文本文件存储数据
loadmodule "db_text.so"

#### 调度器模块 (dispatcher)
# 功能：SIP 目标地址的负载均衡和故障转移
# 用途：通过 ds_select_dst() 选择目标并转发请求
loadmodule "dispatcher.so"

#### UAC 模块 (uac)
# 功能：修改 SIP 消息中的 From/To/Contact 头域
# 用途：代理转发 REGISTER 时替换 Contact，使平台后续消息先发到代理
loadmodule "uac.so"

#### NAT 检测与处理模块 (nathelper)
# 功能：检测和修复 NAT 穿透问题
# 用途：确保位于 NAT 后的客户端响应经过代理
loadmodule "nathelper.so"
# 清空默认的 a=nortpproxy:yes 行,只为能启用 fix_nated_sdp 第 3 参数的
# new_sdp_lines 注入路径(add-no-rtpproxy flag + new_sdp_lines 互不冲突)
modparam("nathelper", "nortpproxy_str", "")

#### XML 解析模块 (xml)
# 功能：基于 libxml2 解析 SIP 消息体中的 XML（GB28181 Catalog 等）
# 用途：自学习设备与通道关系时使用 $xml(path) 替代脆弱的 re.subst 字符串匹配
loadmodule "xml.so"

#### SQL 操作模块 (sqlops)
# 功能：在 SIP 信令处理过程中执行 SQL 查询并把结果写到 AVP
# 用途：把 GB28181 子通道 ID 映射到所属设备 AOR，支持平台→通道的 INVITE 回放路由
# 这里使用默认 DB URL（id=0），指向 db_text 数据目录
loadmodule "sqlops.so"
modparam("sqlops", "db_url", "text:///etc/opensips/dbtext/channel_aliases/")

########### 模块参数配置 ##########

# dispatcher 模块参数：使用 db_text 驱动
# db_url: 数据库连接URL，text:// 开头表示使用 db_text 驱动
modparam("dispatcher", "db_url", "text:///etc/opensips/dbtext/dispatcher")
# table_name: 调度器表名
modparam("dispatcher", "table_name", "dispatcher")

# db_text 模块参数：非缓存模式，检测文件变化自动重载
modparam("db_text", "db_mode", 1)

########### 路由逻辑部分 ##########

# ========== 工具路由：通用 dispatcher 转发 ==========
# 用于：REGISTER、平台→设备、设备响应平台 等场景
# 封装 ds_select_dst + t_relay + 错误处理
route[dispatch_and_relay] {
	t_on_reply("handle_nat");
	if (!ds_select_dst(1, 0, "f")) {
		sl_send_reply(503, "Service Unavailable");
		exit;
	}
	t_relay();
	exit;
}

# ========== 工具路由：通道别名 UPSERT ==========
route[upsert_channel_alias] {
	$avp(sql_del) = "[{\"channel_id\":\"" + $avp(chan_id) + "\"}]";
	sql_delete("channel_aliases", "$avp(sql_del)");
	$avp(sql_ins) = "[{\"channel_id\":\"" + $avp(chan_id) + "\"},{\"device_aor\":\"" + $avp(device_aor) + "\"}]";
	sql_insert("channel_aliases", "$avp(sql_ins)");
	xlog("L_INFO", "GB28181: upsert $avp(chan_id) -> $avp(device_aor)\n");
}

# ========== 工具路由：提取 SDP 关键字段 ==========
route[extract_sdp_fields] {
	$avp(d_c)   = $(rb{re.subst, /c=IN IP4 ([0-9.]+)/\1/});
	$avp(d_m)   = $(rb{re.subst, /m=video ([0-9]+)/\1/});
	$avp(d_set) = $(rb{re.subst, /a=setup:([a-z]+)/\1/});
	$avp(d_con) = $(rb{re.subst, /a=connection:([a-z]+)/\1/});
	$avp(d_dir) = $(rb{re.subst, /a=(sendonly|recvonly|sendrecv)/\1/});
}

# ========== 工具路由：解析 Catalog XML ==========
route[parse_catalog] {
	if ($avp(cat_body) == NULL || $(avp(cat_body){s.len}) == 0) {
		xlog("L_WARN", "GB28181 catalog: empty body\n");
		return;
	}
	$xml(cat) = $avp(cat_body);
	$var(i) = 0;
	while ($var(i) < 256) {
		$avp(chan_id) = $xml(cat/Response/DeviceList/Item[$var(i)]/DeviceID.val);
		if ($avp(chan_id) == NULL || $(avp(chan_id){s.len}) == 0) {
			break;
		}
		$avp(parent_id) = $xml(cat/Response/DeviceList/Item[$var(i)]/ParentID.val);
		if ($avp(chan_id) =~ "^[0-9]+$" && $avp(parent_id) =~ "^[0-9]+$"
			&& $avp(chan_id) != $avp(parent_id)) {
			$avp(device_aor) = "sip:" + $avp(parent_id) + "@" + $fd;
			route(upsert_channel_alias);
		} else {
			xlog("L_WARN", "GB28181 catalog: Item[$var(i)] invalid (chan=[$avp(chan_id)] parent=[$avp(parent_id)])\n");
		}
		$var(i) = $var(i) + 1;
	}
	$xml(cat) = NULL;
}

# ========== GB28181 通道→设备别名查找 ==========
route[lookup_channel_alias] {
	$avp(sql_sel_cols) = "[\"device_aor\"]";
	$avp(sql_sel_filter) = "[{\"channel_id\":\"" + $rU + "\"}]";
	sql_select_one("$avp(sql_sel_cols)", "channel_aliases", "$avp(sql_sel_filter)", , "$avp(device_aor)");
	if ($avp(device_aor) == NULL) {
		xlog("L_WARN", "GB28181: no alias for channel $rU\n");
		return -1;
	}
	xlog("L_INFO", "GB28181: $rU -> $avp(device_aor)\n");
	return 1;
}

# ========== GB28181 设备查询（平台→设备 MESSAGE）==========
route[gb28181_device_query] {
	if (lookup("location")) {
		xlog("L_INFO", "GB28181: forwarding to [$ru]\n");
		t_on_reply("handle_nat");
		if (!ds_select_dst(1, 0, "f")) {
			sl_send_reply(503, "Service Unavailable");
			exit;
		}
		t_relay();
		exit;
	}
	t_reply(404, "Device Not Found");
	exit;
}

# ========== GB28181 INVITE 回放（平台→子通道）==========
route[gb28181_playback] {
	xlog("L_INFO", "GB28181 playback: channel=$rU\n");

	# 通道别名查询
	route(lookup_channel_alias);
	if ($rc != 1) {
		xlog("L_WARN", "GB28181 playback: no alias, falling through\n");
		return;  # 让主路由继续执行标准 lookup
	}

	# 别名命中：重写 RURI/To
	route(extract_sdp_fields);
	$ru = $avp(device_aor);
	remove_hf("To");
	$avp(to_hf) = "To: <" + $avp(device_aor) + ">\r\n";
	append_hf("$avp(to_hf)");
	xlog("L_INFO", "GB28181 playback: rewrite to $avp(device_aor)\n");

	if (lookup("location")) {
		t_on_reply("handle_nat");
		t_relay();
		exit;
	}
	t_reply(404, "Device Not Registered");
	exit;
}

# ========== GB28181 自学习：MESSAGE 响应中的 Catalog ==========
route[gb28181_catalog_learn_msg] {
	if (!($rb =~ "Catalog")) {
		return;
	}
	$avp(cat_body) = $rb;
	route(parse_catalog);
}

# ========== GB28181 自学习：200 OK 中的 Catalog ==========
route[gb28181_catalog_learn_reply] {
	if ($rb != NULL && $(rb{s.len}) > 0) {
		$avp(cat_body) = $rb;
	} else if ($mb =~ "\r\n\r\n") {
		$avp(cat_body) = $(mb{re.subst, /^[\s\S]*?\r\n\r\n([\s\S]*)$/\1/s});
	} else {
		return;
	}
	route(parse_catalog);
}

# ========== 主请求路由 ==========
route{

	# ========== 对话中请求 ==========
	if (has_totag()) {
		if (is_method("ACK") && t_check_trans()) {
			t_relay();
			exit;
		}
		if (!loose_route()) {
			send_reply(404,"Not here");
			exit;
		}
		if (is_method("BYE")) {
			do_accounting("log","failed");
		}
		route(relay);
		exit;
	}

	# ========== CANCEL ==========
	if (is_method("CANCEL")) {
		forward();
		exit;
	}

	t_check_trans();

	# ========== 安全检查 ==========
	if (!is_method("REGISTER") && !is_myself("$fd") && !is_myself("$rd")) {
		send_reply(403,"Relay Forbidden");
		exit;
	}

	if (loose_route()) {
		xlog("L_ERR", "Preload Route denied [$fu/$tu/$ru/$ci]");
		if (!is_method("ACK"))
			send_reply(403,"Preload Route denied");
		exit;
	}

	# ========== 记录路由 ==========
	if (!is_method("REGISTER|MESSAGE"))
		record_route();

	# ========== INVITE 计费 ==========
	if (is_method("INVITE")) {
		do_accounting("log");
	}

	# ========== GB28181 MESSAGE 处理 ==========
	# 外部 MESSAGE（设备→平台）：自学习 + 转发到平台
	if (is_method("MESSAGE") && !($fu =~ "@[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+:[0-9]+") && is_myself("$rd")) {
		route(gb28181_catalog_learn_msg);
		append_hf("P-hint: outbound\r\n");
		route(dispatch_and_relay);
	}

	# 平台 MESSAGE（平台→设备）：查询设备
	if (is_method("MESSAGE") && is_myself("$rd") && !($tu =~ "@[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+:[0-9]+")) {
		route(gb28181_device_query);
	}

	# ========== PUBLISH/SUBSCRIBE 不支持 ==========
	if (is_method("PUBLISH|SUBSCRIBE")) {
		send_reply(503, "Service Unavailable");
		exit;
	}

	# ========== REGISTER ==========
	if (is_method("REGISTER")) {
		$avp(src_ip) = $si;
		$avp(src_port) = $sp;
		xlog("L_INFO", "REGISTER: $avp(src_ip):$avp(src_port) ct=[$ct]\n");
		fix_nated_contact();
		if (!save("location", "no-reply"))
			xlog("L_WARN", "REGISTER failed for $tu\n");
		route(dispatch_and_relay);
	}

	# ========== 检查请求用户名 ==========
	if ($rU==NULL) {
		send_reply(484,"Address Incomplete");
		exit;
	}

	# ========== INVITE 回放（平台→子通道）==========
	if (is_method("INVITE") && is_myself("$fd") && !is_myself("$rd") && $rU =~ "^[0-9]+$") {
		route(gb28181_playback);
	}

	# ========== 标准位置查询 ==========
	if (!lookup("location","method-filtering")) {
		t_reply(404, "Not Found");
		exit;
	}

	do_accounting("log","missed");
	route(dispatch_and_relay);
}


# ========== 中继路由块（relay route） ==========
# 负责将 SIP 请求转发到目标地址（纯代理模式）
route[relay] {
	# 纯代理模式：直接转发请求，不创建事务
	forward();
	exit;
}



# ========== 分支路由块 ==========
# 当 INVITE 呼叫创建多个分支时（如呼叫同时振铃多个设备）触发
branch_route[per_branch_ops] {
	xlog("new branch at $ru\n");  # 记录新分支的目标 URI
}


# ========== 回复处理路由块 ==========
onreply_route[handle_nat] {
	xlog("L_INFO", "Reply: status=$rs mb_len=$(mb{s.len})\n");

	# ========== 200 OK SDP 修正 ==========
	# 注入 a=setup:active 和 a=connection:new（GB28181-2016 规范）
	if (t_check_status("200") && $mb =~ "application/sdp") {
		route(extract_sdp_fields);
		xlog("L_INFO", "PB-200OK: c=$avp(d_c) m=$avp(d_m) setup=$avp(d_set) conn=$avp(d_con) dir=$avp(d_dir)\n");
		fix_nated_sdp("add-no-rtpproxy", "$socket_in(ip)", "\r\na=setup:active\r\na=connection:new");
	}

	# ========== Catalog 自学习（200 OK 响应）==========
	if (t_check_status("2[0-9][0-9]") && $mb =~ "Catalog") {
		route(gb28181_catalog_learn_reply);
	}

	fix_nated_contact();
}


# ========== 失败路由块 ==========
# 当事务失败时触发（如被叫无响应、超时等）
failure_route[missed_call] {
	# 检查呼叫是否被取消
	if (t_was_cancelled()) {
		exit;  # 被取消则不再处理
	}

	# 取消下面注释以拒绝基于 3xx 重定向的呼叫
	# 这可以防止呼叫被重定向到未知目的地
	##if (t_check_status("3[0-9][0-9]")) {
	##t_reply(404,"Not found");
	##	exit;
	##}


}
